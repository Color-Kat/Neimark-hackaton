import { Button as UIButton } from "@heroui/button";
import React, { FC } from 'react';

interface ButtonProps {

}

export const Button: FC<ButtonProps> = ({}) => {
    return (
        <UIButton>sadf</UIButton>
    );
};